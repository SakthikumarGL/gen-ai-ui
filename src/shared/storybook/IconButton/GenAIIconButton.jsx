import { IconButton } from '@material-ui/core';
import React from 'react';
import './genAIIconButton.scss';

const GenAIIconButton = (props) => <IconButton {...props} />;

export default GenAIIconButton;
